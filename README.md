Environment Setup
For the Front End
create a env.local file with your Google maps API key!
inside of it put:
VITE_GOOGLE_MAPS_API_KEY: "Your key here"
to install dependecies: npm install
to run the FE app- npm run dev
